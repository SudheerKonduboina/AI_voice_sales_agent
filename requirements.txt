# ============================================================
# Core — always required
# ============================================================
openpyxl>=3.1
pyyaml>=6.0

# ============================================================
# Agent API
# ============================================================
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# ============================================================
# Development & Code Quality
# ============================================================
ruff>=0.4
mypy>=1.10

# ============================================================
# Testing
# ============================================================
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27         # for FastAPI TestClient

# ============================================================
# Local LLM (optional — install from requirements-optional.txt)
# llama-cpp-python and huggingface_hub have been moved to
# requirements-optional.txt because they are large binary
# dependencies not needed when using Ollama (the primary provider).
#
# To install: pip install -r requirements-optional.txt
# ============================================================

# ============================================================
# Real-time voice pipeline (optional — for --mode live)
# pipecat-ai[whisper,piper]>=0.0.40
# ============================================================
